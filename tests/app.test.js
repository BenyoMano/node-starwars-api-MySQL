import { expect, it } from "vitest";
// import { menu } from "../app";

it('Should rerun menu() when the key provided is not one of the alternatives', () => {
    // const key = 'U';

    // const result = menu(key);

    // expect(result).toReturn(menu());

    expect(2+2).toBe(4);

});